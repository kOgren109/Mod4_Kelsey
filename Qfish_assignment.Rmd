---
title: "QFISH assignment"
author: "Kelsey Ogren"
date: "2024-09-09"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

##Importing the Shark Control Program Data .csv file
```{r importing data .csv file}
df <- read_csv("export.csv")
```


##Data Wrangling and Cleaning
```{r my data wrangling and cleaning}
library(dplyr)
library(tidyverse)
df2 <- df |>
  select(c(...1, CalendarYear, ...3, ...5:...50)) |>
  rename("...2" = CalendarYear) 

df3 <- df2 |>
  row_to_names(row_number = 1)

```

```{r ben's data wrangling}
shark_program <-
  read_csv("export.csv") |>
  row_to_names(row_number = 2, remove_rows_above = FALSE) |> #variable names are meaningless, so change by instructing 'row_to_names' function to put row number 2 as the name
  clean_names() #but we want
head(shark_program)

```



```{r separating species group out}
library(tidyverse)
library(dplyr)
#install.packages("janitor")
library(janitor)

mammal_catch <-
  shark_program |>
  select(area | contains("mammal")) |>
  row_to_names(row_number= 1) |>
  rename(Area = 1) |>
  mutate(across(-c(Area), as.numeric))

shark_catch <-
  shark_program |>
  select(area | contains("shark")) |>
  row_to_names(row_number= 1) |>
  rename(Area = 1) |>
  mutate(across(-c(Area), as.numeric))

turtle_catch <-
  shark_program |>
  select(area | contains("turtle")) |>
  row_to_names(row_number= 1) |>
  rename(Area = 1) |>
  mutate(across(-c(Area), as.numeric))

other_catch <-
  shark_program |>
  select(area | contains("other")) |>
  row_to_names(row_number= 1) |>
  rename(Area = 1) |>
  mutate(across(-c(Area), as.numeric))
```